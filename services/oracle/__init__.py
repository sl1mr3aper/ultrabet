"""Oracle: единый агрегатор сигналов для уточнения вероятностей.

Архитектура:
- ``factors.py`` — отдельные факторы (травмы, форма, мотивация, H2H,
  рыночные «грузы»). Каждый фактор возвращает :class:`FactorResult`
  с дельтами по market_key и confidence.
- ``weights.py`` — вес каждого фактора (можно тюнить эмпирически).
- ``regulator.py`` — :class:`Oracle`, аккумулирует все factor results,
  применяет к baseline-вероятностям и возвращает скорректированные.

Интеграция в ``PredictionService.predict()``:
  Oracle применяется ПОСЛЕ self_learner и CalibrationService — то есть
  калибровка делает вероятности «честными» в среднем по выборке, а Oracle
  добавляет матч-специфичные сдвиги (травмы, форма, H2H и т. п.).

UI/форматирование не меняются — финальные числа просто становятся точнее.
"""

from services.oracle.regulator import Oracle

__all__ = ["Oracle"]
