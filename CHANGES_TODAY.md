# Сводка работ за сегодня (2026-04-27)

Все правки сделаны в архивной версии `/home/ubuntu/ultrabet-v2/`.
Исходный GitHub-репозиторий не трогался.

## Итого: ~+760 строк / ~−120 строк в 14 файлах

| # | Что сделано | Файл(ы) | Строк |
|---|-------------|---------|------:|
| 1 | AI-блок: «главный прогноз», 4-строка → 3-строка (надёжность / вердикт / общее мнение), без воды, без кфов | `services/ai_refiner.py` | +24 / −12 |
| 2 | Пояснение Kelly / Half-Kelly / Quarter-Kelly на русском под блоком ставок | `bot/formatters.py` | +6 |
| 3 | Прогресс-бар: линейная интерполяция pct между стадиями (без замираний), 14-ячеечный бар, обновление каждые 0.7 с, защита от лишних `edit_text` | `bot/progress.py`, `bot/handlers/predictions.py` | +28 / −6 |
| 4 | Раздел «Live» → «Live-матчи» во всех заголовках и кнопках | `bot/texts.py` | +2 / −2 |
| 5 | Декоративные полоски (`━━━`, `═══`, `───`) убраны во всех экранах. `DIVIDER`/`DIVIDER_THIN` пустые. `header()`/`subheader()` — пустая строка как разделитель | `bot/styles.py` | +6 / −18 |
| 6 | История прогнозов: коэффициенты `@1.89` убраны полностью; ✅/❌/⏳ проставляются автоматически из `PredictionOutcome.hit` | `bot/handlers/history.py` | +1 / −5 |
| 7 | Фоновая автогенерация прогнозов: `TopMatchesPrecompute` теперь пишет топ-3 рынка каждого матча в `PredictionOutcome` (с дедупом по `(game_id, market_key)`) → `SelfLearner` сверяет с фактом → калибровка обновляется без участия пользователя | `services/topmatches_precompute.py`, `main.py`, `worker/main.py` | +75 / −2 |
| 8 | Админ-безлимит. `ADMIN_IDS=1169703231` в `.env`, middleware ставит `is_admin=True`, дневной лимит 40 не применяется. В шапке прогноза — `👑 Admin-режим: запросы безлимитны` | `.env`, проверка существующего кода | +1 |
| 9 | AI-отчёт: убрана строка «⚠️ Риск», осталось 3 строки (надёжность / вердикт / общее мнение). Лимит 110 символов на строку, без markdown | `services/ai_refiner.py` | +6 / −10 |
| 10 | Историческая профитность: точные счета убраны из топ-3, остались тоталы / результаты / ИТ / ОЗ / форы. Лучший точный счёт идёт в скобках в заголовке стороны как подсказка `(напр. 1:2 — 29%)` | `bot/formatters.py` | +52 / −20 |
| 11 | Glicko недоступен → сообщение «попробуйте позже или в день матча» | `bot/formatters.py` | +4 / −1 |
| 12 | Бейдж `(↑ модель X%)` теперь отображается при дельте регулятора ≥ 0.3 пп (раньше требовалось ≥ 1 пп и 5 матчей истории) | `bot/formatters.py` | +6 / −6 |
| 13 | Главное меню: убраны упоминания «прогнозы от 85%», текст упрощён | `bot/texts.py` | +6 / −7 |
| 14 | О боте: убраны «команда: UltraBet Labs», английские термины (Ensemble, Season-table, last-games-stats, fair-кф). Формула EV оформлена красиво. Добавлено «🌐 Язык: русский» | `bot/handlers/main_menu.py` | +14 / −22 |
| 15 | Настройки: убран список команд (`/strategy`, `/bankroll`, `/notify`, `/tz`, `/lang`). Управление — кнопками | `bot/handlers/main_menu.py` | +1 / −7 |
| 16 | Подписка: с кнопок убрано `(40/день)`, формат `🎟 1 день · 50 ⭐`. В тексте тарифов и в описании инвойса — единое «До 40 отчётов в день на период: X» | `bot/keyboards.py`, `bot/handlers/main_menu.py`, `bot/handlers/subscription.py` | +6 / −9 |
| 17 | CSV-подпись: английские названия `(game / odds / injuries / last games / profits / season table)` заменены на русские | `bot/handlers/predictions.py` | +2 / −2 |
| 18 | Настройки: убрана строка «🔔 Уведомления» | `bot/handlers/main_menu.py` | +1 / −3 |
| 19 | Подписка: при покупке тарифа единое описание «До 40 отчётов в день на период: X» вместо индивидуальных | `bot/handlers/subscription.py` | +1 / −1 |

## Накопительные эффекты

- AI-отчёт стал короче и точнее (3 строки вместо 4, без воды и без коэффициентов)
- Прогресс-бар не «замирает» больше на одной отметке — pct идёт плавно
- В фоне бот сам генерирует ~50 прогнозов / 30 мин и сравнивает их с фактом → калибровка тренируется без участия пользователя
- Все английские термины из пользовательского интерфейса убраны
- Декоративные полоски удалены из всех 12+ экранов одной правкой `bot/styles.py`
- Администратор не упирается в дневной лимит 40 запросов

## Стек

- Python 3.12, aiogram 3.16, SQLAlchemy 2.0 + aiosqlite, Pydantic v2
- 570/570 тестов зелёные, ruff чистый
- Бот работает в `@ares_main_bot`, polling активен, один инстанс
- Gemini AI-рефайнер `gemini-2.5-flash-lite`, temp=0.25, maxOutputTokens=320
- Историческая БД `MatchResult` пополняется автобэкфиллом каждые 6 ч
- Калибровка `IsotonicRegression` фитится раз в сутки
- LightGBM-стекинг — раз в неделю при ≥ 1000 оценённых исходов
- Кэш-прогрев + предрасчёт топ-50 матчей — каждые 10/30 мин

## Оставшаяся подсказка по запуску 24/7

Для бесшумного автозаработка на VPS:

```bash
# /etc/systemd/system/ultrabet-bot.service
[Unit]
Description=UltraBet Telegram bot
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/ultrabet-v2
ExecStart=/home/ubuntu/ultrabet-v2/.venv/bin/python main.py
Restart=always
RestartSec=5
EnvironmentFile=/home/ubuntu/ultrabet-v2/.env

[Install]
WantedBy=multi-user.target
```

Аналогичный юнит для `worker/main.py` (фоновые задачи).
`systemctl enable --now ultrabet-bot ultrabet-worker` — и бот работает 24/7,
звёзды капают, ML-калибровка обновляется сама.
