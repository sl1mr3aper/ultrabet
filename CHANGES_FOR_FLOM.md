# Правки по фидбеку (2026-04-27)

Все изменения сделаны в архивной версии `ultrabet-v2/`.

## Файлы, которые изменились

| Файл | +/− | Что |
|---|---:|---|
| `bot/formatters.py` | +35 / −20 | Блок «Историческая профитность» → «Положительные тренды», переводы рынков, бадж модели на каждой котировке |
| `services/ai_refiner.py` | +14 / −5 | Промпт AI-уточнения: вердикт строго с фактом из отчёта, без пустого «подтверждаю» |

Тесты: `570/570` зелёных, `ruff check` без ошибок.

---

## 1. Блок котировок (топ-15) — бадж модели

**Было:** `1. ТМ 2.5 — 55.0%  _(↑ модель 47%)_` (только когда сдвиг ≥ 1 пп)

**Стало:** `1. ТМ 2.5 — 55.0%  _(↑ модель 47% → 55%)_` для **каждой** котировки, у которой регулятор вернул `p_model`.

- `↑` если регулятор повысил вероятность (delta ≥ 0.3 пп)
- `↓` если понизил (delta ≤ −0.3 пп)
- `≈` если сдвиг практически нулевой (модель и история совпали)

X% — сырая вероятность по формуле (Glicko + Poisson), Y% — итоговая с учётом калибровки и истории лиги.

`bot/formatters.py:594-617`

---

## 2. Блок «Положительные тренды» (раньше — «Историческая профитность»)

### Было
```
📈 Историческая профитность (последние ~15 матчей в этой лиге)
🏠 Manchester United (дома):  (напр. 4:4 — 5%)
  • Победа → Ничья — ROI +12.55  ·  9/25 (36%)
  • Обе забьют score → Да — ROI +7.02  ·  18/23 (78%)
  • Home/Away → Победа — ROI +6.47  ·  20/23 (87%)
```

### Стало
```
📈 Положительные тренды (последние 25 матчей в этой лиге)
ROI — средняя прибыль с 1 у.е. ставки на этот исход у этой команды;
       доля справа — сколько раз исход заходил из общего числа матчей.
🏠 Manchester United (дома):
  • Исход матча → Ничья — ROI +12.55  ·  9/25 (36%)
  • Обе забьют → Да — ROI +7.02  ·  18/23 (78%)
  • Исход (без ничьей) → Победа — ROI +6.47  ·  20/23 (87%)
```

### Что починили
1. Переименовали блок: «Историческая профитность» → «Положительные тренды».
2. `~15` заменили фактическим максимумом `gamesCount` среди показанных строк (в примере — 25).
3. Убрали усиление-подсказку с точным счётом `(напр. 4:4 — 5%)` из заголовков сторон — точные счета в этом блоке вообще не появляются.
4. Добавили пояснение ROI в первую же строку блока.
5. Английские термины починили в `_PROFIT_MARKET_RU`:
   - `Match Winner` → «Исход матча» (раньше «Победа», что путалось с outcome `Победа → Ничья`)
   - `Home/Away` → «Исход (без ничьей)» (раньше выводилось как есть на английском)
   - `Both Teams Score` → «Обе забьют» (раньше получалось «Обе забьют score» из-за префикс-матчинга)
6. `_ru_market` теперь не приклеивает к переводу нечисловой «хвост»: если после префиксного совпадения остался англ. суффикс типа `score` — он отбрасывается; числовые суффиксы (`2.5`, `1.5`) сохраняются.

`bot/formatters.py:265-303` (словари), `bot/formatters.py:326-347` (`_ru_market`), `bot/formatters.py:373-451` (`_format_profits_block`).

---

## 3. AI-уточнение (Gemini)

### Было
```
✅ Надёжность: xG модель 1.54 против 1.06.
🎯 Вердикт: подтверждаю, ожидается низкая результативность.
💬 Общее мнение: Манчестер Юнайтед атакует, Брентфорд обороняется.
```

Проблемы:
- «подтверждаю» висит без контекста: что подтверждает, на основе чего.
- Вердикт не ссылается на конкретные цифры из отчёта.

### Что поправил в промпте (`services/ai_refiner.py`)
1. Запретил пустые слова без обоснования: «подтверждаю», «согласен с моделью», «всё ок» — больше модели нельзя их использовать.
2. Любая оценка должна содержать конкретное число/факт из отчёта (xG, Glicko, форма, ключевая травма).
3. Переформулировал блоки:
   - **Надёжность** — конкретный факт, который **поддерживает главный пик** (например: «xG 1.54 vs 1.06 — поддерживает ТМ»).
   - **Вердикт** — `брать` / `не брать` / `осторожно` по главному пику, обязательно с цифрой/фактом из отчёта. Слово «подтверждаю» прямо запрещено.
   - **Стиль игры** — короткий вывод о силе и стиле обеих команд и ожидаемом сценарии (атакующий темп / закрытая игра / ставка на одну атаку).

`services/ai_refiner.py:133-165`

### Ожидаемый вид
```
✅ Надёжность: xG 1.54 vs 1.06 — поддерживает ТМ 2.5; Glicko +35 у хозяев.
🎯 Вердикт: брать ТМ 2.5 — суммарный xG 2.60 < 2.5+маржа, обе команды редко забивают.
💬 Стиль игры: МЮ владеет мячом и атакует флангами, Брентфорд закрывается и выходит в контр.
```

---

## Накопительный эффект

- Все английские термины в блоке трендов (включая «score», «Home/Away», «Match Winner») убраны.
- Блок котировок стал прозрачнее: на каждой строке видно, как регулятор подкрутил вероятность относительно сырой формулы.
- AI-уточнение перестанет писать «подтверждаю» без обоснования и будет ссылаться на конкретные числа из отчёта.

## Запуск

На моей VM проект собран и запущен через:
- Ubuntu 22.04, Python 3.12.8 в `.venv`
- `python main.py` → long polling, бот `@ares_main_bot`
- БД: SQLite (`sqlite+aiosqlite:///data/bot.db`)
- AI-рефайнер: Gemini `gemini-2.5-flash-lite` (включён, ключ из `.env`)

После правок я запустил бота повторно — старт чистый, 570/570 тестов зелёные. Сейчас инстанс на этой VM остановлен (чтобы не конкурировать с твоим основным polling).
